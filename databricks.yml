# Databricks Asset Bundle for LeaseMiner
# See https://docs.databricks.com/dev-tools/bundles/index.html

bundle:
  name: fins-lease-extraction

targets:
  dev:
    mode: development
    default: true
    workspace:
      # Set via: databricks auth login <your-workspace-url>
      # Or override with: databricks bundle deploy --target dev -p <profile>
      host: ${DATABRICKS_HOST}
